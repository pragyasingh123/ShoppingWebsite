import { BasePathDirective } from './base-path.directive';

describe('BasePathDirective', () => {
  it('should create an instance', () => {
    const directive = new BasePathDirective();
    expect(directive).toBeTruthy();
  });
});
